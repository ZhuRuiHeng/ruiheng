<!--pages/more/more.wxml-->
<view class="container">
    <!--商品 列表  -->
    <!-- <view class="service-list">
        <navigator class="service-item" wx:for="{{newlist}}" wx:key="{{item.id}}" url="../inform/inform?id={{item.id}}">
            <view class="service-img">
                <image src="{{item.imgurl}}" mode="aspectFill"></image>
            </view>
            <view class="service-text">
                <view class="service-tite">{{item.name}}</view>
                <view class="caozuo">
                    <text class="service-price">￥{{item.price}}</text>
                    <view class="service-gouwu"><image src="{{item.img}}" ></image></view>
                </view>
                <view id="gouwu{{item.id}}" class="buy-response" catchtap="addCar"></view>
            </view>
        </navigator>
    </view> -->
    <view class="more-list">
        <navigator class="service-item"  url="../inform/inform?id={{item.id}}">
        </navigator>
    </view>
</view>
